import React from 'react';
import { Link } from '@reach/router'

const SplashScreen = () => {
  return (
    <main>
      <Link to='map'>Enter App</Link>
    </main>
  );
};

export default SplashScreen;